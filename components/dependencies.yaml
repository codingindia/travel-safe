name: My Group
description: Group for real-time data sharing


version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    implementation: ‘com.google.firebase:firebase-analytics:17.2.2’
    sdk: flutter
    apply plugin: ‘com.google.gms.google-services
    
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
   - assets/images/
